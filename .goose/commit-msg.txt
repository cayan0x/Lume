chore: 治漂——文档/根目录/徽章对齐 + 新规则「文档引用必须存在」

用户点名的三处丑，逐条核实后都成立，已修：

1) 文档漂：ARCHITECTURE.md 点名 host/aux-calls.ts、host/extraction-runner.ts（全仓不存在＝幽灵模块）；
   写 446 条测试/34 文件（实测 573/50）；index.ts 851 行（实测 780）；README 徽章 0.7.0（package.json 0.7.4）。
   → 删幽灵模块；硬数字换成「以 npm test 为准」；徽章对齐 0.7.4；
   → **新增 lint 规则 9**：ARCHITECTURE.md / README.md 里提到的 src|lib 路径必须存在，否则报错
     （文档再漂就在 lint 阶段红灯，而不是靠人肉发现）。

2) 根目录垃圾堆：删掉 0 字节的 `names.includes(n))`（shell 重定向事故遗留）；
   五份 LUME_*.md 计划稿 → docs/plans/；人设素材（docx/md/txt）→ docs/persona/。

3) lib/ 入库、.goose/ 归属：留给下一批（会改发布流程，先问再动）。

校验：573 用例 · lint 9 条规则 0 违规 · 门禁 37/37 · build · install（live 与仓库一致）。
