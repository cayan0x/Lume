# lume host-api approval hooks P2 blocking-gate
【P2 否决式门槛可行性：宿主有原生拦截位（2026-09-23 证实）】证据都在 D:/ProgramFile/DSH Desktop/resources/app/node_modules/@deepseek-ai/ 下：(1) dsh-user-approval（0.1.5-rc.2）——ctx.approval 服务，**approval/request waterfall**（插件可 ctx.on 注册 answerer），outcomes = allowed-once | rejected | cancelled | unavailable，缺 answerer 时 **fail-closed**；会话策略 approval/policy = ask | never（never = 需要审批的动作自动被拒）；审计对必须被 turn 包住（turn/start..turn/end），否则重载时被当崩溃尾巴丢弃。(2) dsh-hook-protocol + dsh-hooks-claude-code / dsh-hooks-codex —— Claude Code / Codex 风格 hook 协议：事件表含 **PreToolUse** / PostToolUse / SessionStart / UserPromptSubmit / Stop / SubagentStart|Stop；BLOCKING_EXIT_CODE = 2（stderr 作为阻断原因回给模型）；matcher 引擎支持 claude-literal（word+pipe）与 regex 两种方言。(3) 另有 dsh-tool-ask-user（模型向用户提问的载具）、dsh-repeat-tool-reminder、dsh-fs-observation-policy、dsh-invariants 等；前端已有 dsh-client-ui-approval 审批交互。**结论**：Lume 想做「真拦截」（拦未读文件就改、拦破坏性命令）技术可行；但它绑定的是「拦什么」的策略决定，且误拦会卡住用户真实工作，因此本批未实现，等用户拍板。

