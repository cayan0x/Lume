# lume release rule
Lume（lume-dsh-plugin，D:\Projects\Plugin\Lume）：改完只到「提交 / 停在待确认」为止，禁止自行 tag / push / npm publish，必须先等用户确认。宿主接线类改动（RPC 注册、注入作用域、apply 兜底）单测抓不到，必须真机重启 DSH 后看 %APPDATA%\logs\harness.log 里 `lume: RPC 通道` 那行。发布：npm run release:check → npm run release:publish（两阶段，next→latest）。详见仓库 RELEASING.md 与 `.goosehints`。

# lume handoff goose-memory
Lume 断点续接：本项目（D:\Projects\Plugin\Lume）的跨会话记忆就存在仓库内的 `.goose/memory/*.txt`（goose memory 扩展的 local scope，按工作目录归属，一个 category 一个文件），用 `retrieveMemories(category:"*", is_global:false)` 一次读完；会话收尾用 `rememberMemory(is_global:false)` 追加。开场再做：读 `.goosehints`（自动注入）、`.goose/state.md`、`git log -5 --stat` + `git status` + `npm dist-tag ls lume-dsh-plugin`。

# handoff state 0.8.0 unpushed verification
【断点续接 · 2026-09-23 10:40】当前状态（已核对，见 .goose/state.md）：
- **已发布**：npm `latest = 0.7.4`（已弃用 0.6.2/0.7.0/0.7.1/0.7.2；0.7.3 与 0.7.4 正常）
- **未发布**：工作区已有「0.7.4 + 两个新能力层（设计 pass / 需求锚点）」，`package.json` **仍是 0.7.4 未 bump**
- **git**：`main = bd37f0c`，**5 个提交未推送**（远端停在 2351783）：`58f4061` 设计层 · `9a5ae7d` 需求层 · `e11e9e8`/`3ad0097`/`bd37f0c`（门禁修正 + 启动能力标记）
- **本地验证**：358 用例全绿 · tsc 干净 · 构建通过 · **发布门禁 21 条全过**
- **真机**：本机 20 个 live generation 已覆盖为当前构建（live = `lume-dsh-plugin+0.7.4+ad869a38841c`）；**用户是否重启 DSH 未确认**
- **重启确认**：harness.log 里应有 `lume: 已加载（…，能力=载具+触发器+设计pass+需求锚点）`；没有这行＝跑的还是旧代码
- **装本机**：`node scripts/install-local.mjs`（自动备份 %TEMP%，lib 镜像 + assets 覆盖）
- **下一步**：① 等用户重启并验证（4 个信号：〔需求锚点〕/〔需求解读〕/〔需求漂移〕/〔设计三问〕）② 通过后 bump **0.8.0**（新能力层，建议大版本递增）→ CHANGELOG → `npm run release:check` → `npm run release:publish` → push（含 5 个未推提交）+ tag
- 若"注入的提示仍被忽略"：升级为**首次改动前的交付门槛**（需求对照清单）或漂移命中时中断计划要求重述

# lume handoff restart rpc
【断点续接更新 · 2026-09-23 10:45】用户重启已确认生效（10:29:25 完全重启；harness.log 10:29:39 出 `能力=载具+触发器+设计pass+需求锚点` 行；generation lume-dsh-plugin+0.7.4+ad869a38841c 的 lib 与仓库构建逐字节相同）。**RPC 通道那行的正确读法**：`RPC 通道 /lume = rpc.handle 失败(cannot get property "webServer" without inject) | webServer.register(自注册路由)` 是**成功状态**——主路径按 DSH Desktop 0.9.1 的 cordis inject 限制必失败，回退自注册成功（notes 只在 register 未抛异常时才 push，且两路全失败时还会补一行 `shapes: …`，没出现那行就等于通道是活的）。外部 HTTP 探测验证不了：本机 web server 全站 401 `Pair your phone again.`（全局 pairing guard）。剩下的是**行为验证**（4 个信号）→ 通过后 bump 0.8.0 → release:check → release:publish → push（含 5 个未推提交）+ tag。详见 .goose/state.md。

# lume architecture lint-arch session-runtime-grouping distill-split pending-restart unpushed
【架构整理 1→5 完成 · 2026-09-24 · 4 个提交未 push · 待重启】① index.ts 再瘦：实现搬进 host（新增 host/llm-aux.ts 的 callLlm、host/project-access.ts 的载具/项目知识读写入口）→ 1071 → 878 行（原始 1723，累计 −49%）。② 架构检查器 scripts/lint-arch.mjs（零依赖，npm run lint 并串进 release:check；规则：分层/相对 import 必须带 .js/src 禁 console/void X.then 必须 catch（可写「已吞异常」豁免）/禁 @ts-ignore；首跑抓 7 处含 1 处真漏 catch）。为什么不用 eslint：本仓 devDeps 有既存 peer 冲突，装它要 --legacy-peer-deps，不值得动依赖图与 lockfile——规则可直接翻译成 no-restricted-imports/no-floating-promises/no-console。③ ARCHITECTURE.md（三层职责/提示三通道与成本模型/六个判据机制/加机制 7 步清单/三层护栏/短板清单）。④ SessionRuntime 45 → 39 字段：工具与证据 7 字段（evidence/artifactText/inspectedTargets/seenSymbols/lastToolName/Args/Target）收进 st.agent（它们一起被清空、一起被读取）。⑤ host/distill.ts 560 → 239 行：纯字符串的提示词+解析+归一拆到 host/distill-prompt.ts（旧名字在 distill.ts 重导出，测试导入改指新模块）。未做：session-events.ts 仍 460 行（单个事件处理器 switch，切开引入更多跳转，等真实痛点）；client/ 未动（UI bundle 测试薄）。验证：437 用例/tsc/lint 全绿，门禁 31/31，install-local 覆盖后 live 与仓库产物逐字节相同，零缓存成本。

# lume architecture deps-typing any-elimination llm-route-bug deep-refactor pending-restart unpushed
【架构整理 ①→⑤ 全完成 · 2026-09-24 · 提交 be20448 + 9069910 · 未 push · 待重启】按「架构/优雅度 9 分」标准一次做到位。① 依赖边界类型化：SessionEventDeps 拆成七个按域命名的接口（env/notice/carrier/signal/prompt/tool/agent，extends 组合但**访问保持扁平** deps.contractOf）；类型一律从模块派生（typeof mod.fn / ReturnType<typeof createXxx> / Pick<ProjectAccess,…>）。新增 host/host-context.ts（宿主 ctx 最小面 + HostPayload）、host/config.ts（LumeConfig 独立成模块，避免 host 反向 import index）、host/llm-route.ts。全仓 any 164 → 24（session-events 73 → 1）。★ 顺带修一个真 bug：index 的 let llmRoute 以值拷进 deps，会话事件在 request/context 里更新的是那份拷贝 → index 侧永远拿到启动时那份（设置回落失败时为 null）→ 提取/蒸馏静默不工作且与「模型没触发」无法区分；改成 LlmRouteCell 共享可变单元。② 删 6 个死导出（knows / trigrams / MEMORY_POINT_CAP / hasUnverified / DISTILL_STAGES / customToRecord）。③ distill 兼容重导出收口（不留过渡态）。④ ARCHITECTURE.md 重写：新增「依赖注入与类型边界」、模块表、护栏表（6 条 lint 规则 + 446 测试）、client 层规则、短板清单。⑤ 深度：session-events.ts 490 → 274 行（turn/end 的 84 行 → host/turn-boundary.ts；依赖契约独立成 host/session-deps.ts，三处处理器共用）；client 层把蒸馏弹窗的「任务状态 → 界面动作」抽成 client/distill-job.ts 纯函数 → client 层第一次有单测（+9 条）。护栏新增 lint 规则 6「类型边界」：裸 any 只允许在 src/index.ts + host/host-context.ts。验证：446 用例（+9）· tsc 干净 · lint 六规则 0 违规 · 门禁 31/31 · install 后 live 逐字节相同；不动协议正文 = 零缓存成本。自评：结构 9/10、优雅 8.5/10（剩余见 ARCHITECTURE.md §7）。

# lume architecture wiring client-layer tests ci lint rescore pending-restart unpushed
【架构整理 6 项一次做到位 · 2026-09-24 · 6 个提交未 push · 待重启】①⑥ 依赖装配集中到 host/wiring.ts：三个 deps（107 项依赖，其中 75 项是无状态函数）从 index 搬出，无状态函数由 wiring 自己 import，只把 index 的真实运行期状态经 WiringInput 传入（ctx / 存储句柄 / 会话态 / 模型路由 / access / 配置开关），顺手搬走 projectOf/projectStore/documentDirective 三个 index 闭包 → index 851→755 行。② 六个接线模块补直接测试（text/sections/bootstrap/project-access/tools/session-events，+39 条）→ 494 用例；覆盖 request/context 更新共享路由 cell（值拷贝丢失更新 bug 的回归）、四域降级、项目键只在真 cwd 缓存、工具 schema 必填是 expectCount、tool/call 自动落账与证据记账。③ client 层整理：新增 client/graph-layout.ts（tokenize 中文二元组 / jaccard / filterByAge / relTime / rgba·hexGlow·brighten / wrapText / buildGraph rand 可注入 / forceStep 纯数学 / hitTestAt）+ client/dom-utils.ts（exportFilename 保留中文、防路径穿越、去前导点、截断 60）→ memory.tsx 263→184 行，+15 测试。④ lint 规则 7 禁 as any（当前 0 处）。⑤ CI（.github/workflows/ci.yml：lint → test → build + 门禁；--legacy-peer-deps）。验证：509 用例 / tsc / lint 七规则 0 违规（60 文件）/ 门禁 31/31 / install 后 live 逐字节相同；不动协议正文 = 零缓存成本。重新评分：架构 9.4、优雅 9.0、综合 9.2（到 9.3 最后一点在 client JSX 重排：distill.tsx 最深 16 层嵌套 + 190 行长行）。

# lume state incident-fixed pending-restart unpushed next-client-jsx
【Lume 当前状态 · 2026-09-24 晚】最后一次动作：commit d4a49ce 修复「客户端重复声明导致 Harness 起不来」（distill.tsx 残留 TEXT_CAP/CHAT_TEXT_CAP + memory.tsx 缺 GRAPH_H 导入），并把 npm run lint 扩成「架构检查 + tsc -p tsconfig.json」（覆盖 src+client+test），门禁新增 client-bundle-parses 与 client-no-duplicate-decl 两条产物断言（共 33 条）。验证：509 用例、lint 0 错、构建通过、门禁 33/33、install-local 已刷新全部 20 个 generation 且真机产物与仓库逐字节一致、可解析。**待用户重启验证**。累计约 22 个提交未 push（远端 origin/main 落后）。未做的尾巴：client 三个组件的 JSX 重排（distill.tsx 最深 16 层嵌套、190 行长行）——这是评分从 9.2 到 9.3 的最后一点，属于机械但需人工的排版工作。

# lume facts cross-session-knowledge cwd-from-snapshot auto-distill sensitive-filter landing-verified-pending
【项目知识跨会话真正落地 · commit 137877b · 待重启验证】根因（钉死）：DSH 0.9.1 里 request/context、request/header、tool/call 都不带 cwd，exec 与提示词上下文的 agent.session 也没有 cwd 字段 → projectKeyOf(空) 返回 null（拒绝写跨会话表防串味）→ facts 从 0.7.x 起真机一次都没落地（模型 3 次 lume_project_note 全丢）。cwd 只在一处：经 user/message 通道投递的运行时快照文本（… session workspace: 后面跟带引号的绝对路径），而那段一直被当非用户消息跳过。实现：① host/host-events.ts 的 workspaceFromSnapshotText()（只认 session workspace 句型，兼容单双反斜杠与 POSIX）；② session-events 在 isUserAuthored 之前解析快照 → 学到 cwd 立刻 flushPendingFacts + 日志「工作目录已解析（来源：运行时快照）」；③ 自动沉淀 core/knowledge.ts extractKnowledgeCandidates（build/test/convention/deadend；必须有证据锚点：路径/文件名/表名字段/命令；排除建议、问句、宿主快照、用户原话；每会话 ≤4 条）；④ 敏感硬拦 looksSensitive（带值的 password/secret/token/私钥/连接串/ENC 标记/实测可解不入库；区分凭证名与凭证值——-Djasypt.encryptor.password 保留、password=ENC(...) 挡掉）；⑤ 项目知识注入每条带新鲜度（刚记/N 小时前/N 天前）；⑥ 历史 unknown 桶里「某私钥实测可解」已剔除（备份在 %TEMP%）。测试 524（+17，44 文件），写测试时抓到 3 处判据漏洞（文件锚点被中文括号挡住、把宿主快照当事实、问句被当知识）。验收四条：日志「工作目录已解析」「自动沉淀候选」「项目知识补落盘 N/M 条 → 键」；lume_project.json 的 facts 出现非 unknown 键的新行；新开同工作区会话开局注入直接带上这些知识。

# lume facts auto-distill cwd tool-result-shape acceptance-pending
【facts 落地链条的两个形状 bug 已修 · 待重启验收】① cwd：只在运行时快照文本（user/message 通道）里，已在 isUserAuthored 之前解析 ✔ 真机日志出现「工作目录已解析（来源：运行时快照）→ D:\Projects\zjhc\b2i-all」（5 个会话）。② 工具结果文本深一层（见 editing-robustness 记忆）✔ 已修。③ 沉淀判据已降噪（去脚手架行、剔纯路径行、剔片段引用），真机精度：545 条结果 → 退费 26 / 优惠视图 14 条候选，抽样基本都是真约定。重要澄清：用户以为「退费新开会话能识别」是插件的功劳，其实是他自己写的 doc\工作区约定.md 第 15 行（继续退费需求前必须先读 会话记忆.md）被 grep 到；插件的 facts 桶此前一条新行都没有。验收四条：日志出现「自动沉淀候选（convention）」与「项目知识补落盘 N/M 条 → 键」；facts 桶出现非 unknown 的目录键新行；新开会话开局注入直接带上这些知识（不再依赖用户手写约定）。527 用例 / lint 0 / 门禁 33/33 / live 逐字节一致。

# lume task-memory session-backfill context-overflow cold-start-injection acceptance-pending
【会话记忆 + 会话补蒸馏 · commits 152ed0d/8270554 · 待重启】解决「上下文撑满 → 压缩失败(context overflow) → 会话聊不动 → 进度丢失」。两条腿：① 补蒸馏 host/backfill.ts：每次插件启动扫最近 7 天会话文件（含已撑满的），机械提炼 → 按工作目录写入跨会话知识；分片执行(每片一个会话，避免阻塞宿主同进程)、幂等(addFact 相似度去重)、失败静默。已一次性补出 139 条(真机 17 个会话)。② 会话记忆 core/task-memory.ts + 项目域新表 task_memory：buildTaskMemory 把 目标/需求原话/已拍板/改动(未验证带[已改未验])/未决/死路/关键定位 压成记忆；renderTaskMemory 注入(含「继续 X」指令 + 同目录其它会话标题)；renderTaskMemoryMarkdown 人读版(尚未落盘到工作区)；isColdStart(无契约/台账/锚点)；contextPressure(75%/90%) + buildContextPressureDirective。接线：session-events 的 session/title 分支、request/context 记 contextWindow、turn/end 每轮导出(fire-and-forget)、assistant/message 压力预警(先落盘再劝换窗口)、prompt-blocks 冷启动注入。546 用例/门禁 35/35/live 逐字节一致。踩坑：存储域强制表名 /^[a-z][a-z0-9_]*$/，taskMemory 被拒 → 必须 task_memory（7 个测试文件加载失败才发现）。未做：markdown 导出到工作区、知识作用域(repo/需求)、更精确版本覆盖旧版、P2 真拦截。

