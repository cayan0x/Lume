# requirements design carriers field-evidence 0.8.0
【新增的两层能力（未发布，待在真机验证）】

**动机（2026-09-23 现场追踪 B2I 优惠视图与订单属性会话，用户三次指出"笨/需求理解不到位"）**：
- 需求写「新增权限人字段」，模型论证「复用 create_id/modify_id」→ 用户：「这明显就是新增字段…没有代码设计架构的思想」
- 需求写「业务类型新增三个选项」，模型推论「删除/割接」→ 用户纠正（它自承认「删除是我脑补的」）
- 一开始抛一堆问题 → 用户：「你只要分析清楚字段的值从哪来就行，怎么还有这么多疑问？」
- 关键数据：这段会话 **契约 0 次调用**（而〔先量化后动手〕被注入 14 次）→ 结论：**注入的散文提示会被忽略，只有插件自己落账才可靠**

**实现的四件（全部有 apply 层测试）**：
1. **需求锚点自动落账**（插件在 user/message 里写，逐字、首条永久保留）→ 每轮回显 `〔需求锚点｜用户原话，逐字保留〕`，并写明"理解必须能追溯到这些句子"
2. `〔需求解读〕` 三条硬规则：需求写死的选择照做（不得重新论证）/ 不得引入需求没提的变更类型 / 问题预算（先读代码，只问代码答不了的，≤2 个并给默认假设）
3. `〔需求漂移〕` 词法检测：模型输出含需求原话没有的变更类型词（删除/删掉/替换/割接/回滚/重构/迁移…）→ 顶一句（`unrequestedChangeWords`，在 core/signals）
4. `〔设计三问〕` + `lume_design` 载具（跨轮回显）+ `〔设计缺失〕` 触发器（摸到 6 处代码仍未写设计）——**且设计三问以"需求已明确的选择照做"为前提**（我初版写成"计算即得 vs 落库冗余"，恰恰鼓励了模型重新论证需求，已修正）

**验证协议**：重启后看 harness.log 的能力行 → 再看 4 个标记（需求锚点/需求解读/需求漂移/设计三问）是否出现且行为是否改变。若"提示仍被忽略"，升级路径：把需求对照清单变成**首次改动前的交付门槛**，或漂移命中时中断计划要求重述。

# lume verification restart requirement-anchor design-pass
【真机验证通过 · 2026-09-23 10:29 重启】重启已确认生效，新两层（设计 pass / 需求锚点）在真机上跑起来了。三条硬证据：① DSH 5 个进程 StartTime=10:29:25/10:29:41（真重启）；② 10:29:39 harness.log 出 `lume: 已加载（…，能力=载具+触发器+设计pass+需求锚点）`（该行是 bd37f0c 新加的，旧构建没有）；③ live generation 的 lib/index.js 与仓库构建逐字节相同（75463 B）。④（行为级）b2i-all 会话 session-c8856bf9 在 10:36:53 那轮注入的〔设计三问〕**带上了 0 号条款「先划边界：需求已明确的选择一律照做，不得重新讨论」**——`git grep` 证明该句只在 9a5ae7d 之后存在、58f4061 没有 → 真机跑的就是含需求层的最新构建。⑤ `harness/storages/lume_project.json`（10:36:53 写入）的 requirements 表出现该会话锚点（用户原话逐字），**自动落账这条路真机生效**（不再依赖模型调工具）。**时序坑（重要）**：锚点在 user/message 事件里异步落盘，同轮注入可能早于落盘几毫秒（实测注入 .857、落盘 .861）→ 用户当轮看不到〔需求锚点〕，**下一轮才回显**；所以「某轮没有锚点」不等于没生效，要再看一轮。

# lume diagnosis drift-false-positive prompt-contradiction sycophancy
【风控式提示的反效果 · 2026-09-23 真机实测】b2i-all 会话 turn9–13（用户反馈『还是不够聪明』）证据链，务必据此改，别再只加散文提示：① **〔需求漂移〕在抠字眼**：词法检测**扫的是 assistant/message 全文（含 reasoning）**，最近 5 轮里 3 轮命中（迁移 / 替换+割接+回滚+迁移 / 替换+割接+迁移），而语境全正当——用户自己就问『业务类型删了旧值，旧数据是不是涉及割接』、模型陈述『该字段 2025-03-17 引入时没做数据迁移』、风险分析；**没有一处是模型要私自删/割接**。代价在推理里直接可见：turn10「系统提示'需求漂移'…要收回」、turn11「**不提割接/迁移/替换**」、turn13「不提迁移/割接/替换/回滚。'覆盖'…**为了安全我换措辞**」→ 模型开始**优化词表而不是解决问题**，并丢掉判断风险必需的词（用户问的正是割接）。改法：只扫可见回答、加否定/疑问语境豁免、提示语去掉『收回』、每会话限次。② **同轮注入自相矛盾**：seq=318 同时有『当前模式：问答，不要调用工具』与『〔先量化后动手〕本轮是任务型请求，开工前先写任务契约（lume_contract）』→ 模型只能赌（选了别调工具）；13 轮 lume_contract / lume_design 各 0 次。根因：carrierBlocks 的 isTask 没跟已定路由（只有 buildImpactDirective 写了 `isTask && mode !== "question"`）。③ **〔需求解读〕规则 1 被泛化成『用户说的都对』**：turn11 开口『对，你这个判断对』，用户 turn13 点破『不是按照我的判断，你也要判断』→ 需要一条『独立判断』硬规则（用户的技术结论 ≠ 需求，允许有条件同意并写风险）。④ **缺证据来源纪律**：turn10 四连 `git log/show/grep *.sql` → 用户『我不明白你为啥要看提交记录』→ 规则应是『判断现状以现有代码为准；git 历史只用于确认引入时点/有无刷数脚本，并说明用途』。⑤ 反证模型本身行：turn13 自己给出了『有条件同意 + 整行 update 会把其它列刷空 + 判空要 isNotEmpty』，是被点破后才做到 → 问题在提示层。⑥ 小失真：重启后才开始记锚点，所以标着『1.（原始需求，最重要）』的其实是会话中段一句问话。

# lume p0 drift-noise route-consistency pending-restart
【P0 已实施 · 2026-09-23 11:10，未提交，等重启验证】针对『还是不够聪明』的诊断，用户只批了 P0（两项都在注入面，不动存储 schema）：(A) **漂移检测降噪**：`core/signals.ts` 新增 `isDriftProposal()` —— 变更词附近有否定/疑问/风险语境（不·没·别·如果·是否·风险·影响·方案…）→ 不计；变更词**前 24 字内**有计划线索（要·会·将·建议·应该·需要·改为…）→ 才算『提议』；语料从锚点扩到**用户侧原话全集**（锚点 + recentUserQueries + userText）；`DRIFT_NOTICE_MAX=2` + `st.driftWordsReported` 限次且同词不重报；`core/text.ts` 新增 **visibleText()**（排除 reasoning/thinking）——本次事故正是扫推理文本，模型于是在推理里躲词；文案去掉『收回』。(B) **提示跟随已定路由**：`carrierBlocks` 新增 `taskMethods = mode !== "question"`，问答轮不再出现〔先量化后动手〕〔设计三问〕〔改动影响面〕；`buildRequirementMethodDirective(taskMethods)` 拆两版（问答轮只留边界规则）。**刻意回退的一处**：触发器（tool 事件流）的 isTask 判据**没有**加 mode 门——触发器是轨迹纠偏，且事件流里 `st.interactionMode` 会滞后（加门会误杀设计缺失/收敛提醒）。验证：368 用例全绿（+10，含三条真实误报原句的反例）· tsc 干净 · 门禁 21 条全过 · install-local 覆盖 20 个 generation。**未做**：P1（自动推进 verified / 红立即闭环 / 首改前定位门槛 / 交付对账列条 / 独立判断+证据来源纪律）与 P2（否决式门槛，需宿主 approval 钩子，未证实）。**遗留观察点**：`classifyInteraction` 把『新增 X 字段/接口』这类需求陈述判成问答时，设计 pass 不会出现（needsDesignPass 要求 mode !== question）。

# lume p0 verification drift-noise route-consistency
【P0 真机验证结果 · 2026-09-23 11:22】重启 11:17:48 / harness.log `lume: 已加载` 11:18:00.745Z；live 的 lib/index.js 与仓库构建逐字节相同（mtime 11:13:08），signals.js 有 isDriftProposal/DRIFT_NOTICE_MAX、methods.js 里旧的「收回并只按需求做」已消失 → 跑的就是 P0 构建。会话 session-c8856bf9（turns 15–18，430 事件）核对：① **躲词 P0 后 0 次**（`不提/为了安全/换措辞/收回` 全命中都在 P0 前的 turn 3/9/10/11/13）；② **问答轮「问答+先量化后动手」同轮出现 0 次**（重启后问答轮注入只剩〔路由〕〔阶段〕〔需求锚点〕）；③ 漂移重启后 0 次触发（这两轮没有提议型句子）→ 仍活只由单测证明（『建议删掉旧值』→触发），限次机制真机未触发过；④ 执行轮 3 步全带全套方法块，没被误砍。**新观察**：讨论轮仍带全套方法块（taskMethods 只排除 question），建议微调为『契约块只在 execute/diagnosis，设计三问/影响面保留在非问答轮』；**载具依旧 0 次调用**（重启后只有 grep/read，也还没有 edit/write → 用户还没开始写代码）→ 这正是 P1（自动推进 verified / 交付对账列条）要解决的：不能指望模型主动调载具。

# lume gap-list project-knowledge-loss p1 next-steps
【待修清单 · 2026-09-23 11:27 第 19 轮核验】① **项目知识被静默丢弃（最该修）**：b2i 会话模型主动调了 3 次 `lume_project_note`（turn 3/8/10：bus_type 引入时点+无刷数脚本、A 版并行实现 WtpfGoodsPropertyDef_A、模块链路），harness.log 对应 3 条『项目知识未落盘（无法确定工作目录）』（09:51/10:15/10:38），`lume_project.json` 的 facts 表**只有历史的 unknown 键**，本项目一条都没有 → 根因是 cwd 未就绪时按设计直接丢弃，而 cwd 在同一轮稍后就能拿到（`st.cwd` 有 prompt 上下文兜底）→ **丢得太早太永久**。修法：待落盘队列 + cwd 已知后补落盘。② **讨论轮仍塞〔先量化后动手〕**：第 19 轮用户问 4 个技术问题 → 路由=讨论，注入却带 先量化+设计三问+影响面（噪音，这轮不该写契约）→ 契约块只在 execute/diagnosis，设计三问/影响面保留非问答轮。③ **载具依旧 0 次**（design/ledger/hypotheses 空，contract 无本会话键），模型仍只 grep/read/pwsh，**还没开始写代码** → P1-C3/C1/C4 趁没开始写正好上。④ **澄清：P0 无回归**——第 19 轮 reasoning 里的『收回』是它自己认错（『我收回原来的例外』），不是躲词，这几轮也没有〔需求漂移〕注入。⑤ **正例**：模型自己会讲证据边界（『数据库类型代码库确认不了，要看配置中心/问运维，不能猜』）→ 『证据来源纪律』硬规则优先级可降，真正要补的是『先读现有实现、别被 git 历史带走』那半条（turn 10 的实际跑偏）。

# lume citation-gate wrong-conclusion evidence-discipline p1
【新型失败：证据错位 + 假选择（2026-09-23 turn18→19）· 待修清单第 0 项】模型在 turn 18 判『**例外是 status（优惠状态）**：它不是人工填的，系统有按生失效时间自动置无效的逻辑（`WtpfGoodsPrepertyDefServiceImpl:159-160` 注释 + `DiscountOrProductOverTimeTask`）…这一列接不接 Excel **要你定**』；用户反问『优惠状态是导入的吗，能导入为啥不能修改？』，模型回读后承认『**我上轮说错，收回**』，正确证据是 `:526-534`（FIELDSTATUS 校验 + `setStatus(tmp.get("优惠状态"))`）。**真实代码核对**（文件 933 行）：159-160 那段注释在 `if (resultCode.isEmpty())` **单条新增分支（150-166）**里，534 行才是 **Excel 导入路径** → 失败性质 = **拿另一条路径的证据推断这条路径的现状**，并把**未核实的前提**包装成决策项让用户拍（成本：用户多花一轮纠正 + 一个假选择）。**现有机制拦不住**：没违反任何规则（提问 ≤2 个、附了假设），而且它**引用的是真实存在的行号**——所以再加『要核实』的散文没用（它以为自己核实了）。**正解 = 引用-证据对齐（citation gate）**：插件维护本会话证据索引（read 的 (文件,窗口) + 工具结果里的 `path:line` 命中），检查可见回答里每个 `文件:行` 引用是否落在索引里，不落在就顶一句**复述事实**（『你引用 X:159-160 不在本会话读过的范围里（该文件读过 412-433/432-486/470-609）——先打开它再下结论』）。验收用例就在今天的数据里：**turn 18 命中**（159 从未读过）、**turn 19 不命中**（526-534 读于 turn 2 窗口 470-609）。工程约束：只对本会话碰过的文件生效、每轮最多一次、只检查含排除性/决策性词（例外/不能/不支持/要你定/需要你拍）的句子。**配套便宜规则**：把决策抛回用户前，前提必须是从这条路径的代码读到的；未核实的推断不能当选项。

# lume 0.7.5 citation-gate auto-verify pending-facts pending-restart
【0.7.5 全量批次已实施 · 2026-09-23 11:45 · 未提交，待重启验证】用户批『一次全做，一块验』，五项全落在**插件能自己算**的判据上（不再加散文）：(A) **引用-证据对齐**：新模块 src/core/citations.ts（EvidenceIndex = 文件→读到过的行窗口；recordReadArgs 只对 read/view 类工具记窗口、recordResultText 记 grep 结果里的 path:line；extractCitations 用纯 ASCII 正则粗扫『名字:行(范围)』再靠索引解析简名；shouldCheckCitations 只在出现 例外/不能/要你定 等排除性决策性措辞时才查；unsupportedCitations 返回『本会话碰过、但该行这次没看过』的引用）+ index 接线（assistant/message 时算，CITATION_NOTICE_MAX=3）；顶的是**复述事实**：『你引用 X:159-160，本会话读过的是 412-609』。**教训**：原先用中文标点字符类 + lookahead 的正则在本仓返回空数组 → 改成 ASCII 粗扫 + 事后过滤才通过测试。(B) **项目知识暂存补落盘**：st.pendingFacts（≤8）+ flushPendingFacts() 在 computeTurn/turn-end/session-disposed 三处触发，cwd 未知不再丢弃（现场：3 条硬知识全丢）。(C) **讨论轮契约块收敛**：contractMethods = execute||diagnosis（taskMethods = mode !== question 保留给设计三问/影响面）。(D1) **自动推进 verified**：ProjectStore.verifyChanges() + settleVerification()，真验证（isRealVerifyCommand：tsc/test/lint/build，git grep 不算）或回读改动文件 → 把 done 自动推成 verified，verify 字段写『自动：命令 → 结果首行』；(D2) 真验证失败立刻顶先修红（不等 3 连击）；(D3) 首个 mutate 时没读过目标文件 → 顶〔先定位〕；(D4) turn/end 有未验证项 → 下一轮列出具体条目。(E) 两条硬规则进 thinking.ts（完整版+推理版）：独立判断（用户的技术结论 ≠ 需求，允许有条件同意）、证据来源纪律（现状以当前代码为准、引用的行必须这次打开过）；methods.ts 需求解读加『提问的前提必须已核实』。验证：**389 用例全绿（+21）· tsc 干净 · 门禁 21/21 · install-local 已覆盖且 live 与仓库 lib 逐字节相同**。重启后核对五件事：引用核对 / 自动 verified / 红立即闭环 / 首改定位门槛 / 讨论轮无先量化。

# lume question-discipline always-on-rule false-questions pending-restart
【提问纪律 · 2026-09-23 12:35 已装本机待重启验证】用户投诉『他会给我提出不存在的问题干扰我，准确性太低』。现场（b2i 会话 turn 20→21）：用户只问『现在方案是不是都清楚了？』，模型回 4 条待你定（生产库类型 / status 双口径 / 权限人下拉来源 / 分页 total）；用户反问『分页还能有疑问？不就是改前端的吗』，模型下一轮认账『问了四个，其中至少两个是我自己造出来的疑问』『三个是我自己造的，撤』，核实后撤三条只留 status。**关键证据**：turn 21 推理逐字写『用户在质疑我的四个不确定。系统提示：提问的前提必须已核实；未核实的推断不能包装成「要你定」的选项』——而四个问题是 turn 20 生成的，当时 seq=463 注入里 含「提问的前提必须已核实」= false（该规则只挂在〔需求解读〕上，而需求解读只在用户给了新需求的轮次出现）→ **规则在场它才自查；问题产生那一轮规则不在场**。**修法**：① thinking.ts 恒定协议（完整版+推理版）新增 P1 提问纪律（先核实前提 / 能定的定下并写默认假设 / 待确认≤2 / 不许把『我没查环境·我猜』列成待你定）；② core/signals.ts countOpenQuestions() 结构计数（待定|待确认|还没定|不确定 小节标题后紧跟的列表项数）+ MAX_OPEN_QUESTIONS=2；③ methods.ts buildQuestionAuditDirective() + index 接线（每会话≤2）；④ 需求解读补同义规则。**同时排除误报**：这两轮〔引用核对〕〔交付对账〕〔先定位〕〔验证失败〕一次都没触发——胡扯问题来自模型自己。验证：395 用例全绿（+6，含 turn20/21 真实文本用例）· tsc 干净 · 门禁 PASSED · install-local 已覆盖且 live 与仓库逐字节相同。**纯函数坑**：加粗标题会被 [-*•] 当列表项（多算一项）→ 列表正则需加 (?!星号) 负向前瞻。

# lume question-audit evidence-based no-quota-wording pending-restart
【提问核对：从「数量」改「证据」+ 去配额化 · 2026-09-23 14:1x · 已装本机待重启】现场（重启后 turn 22→23）：模型答「文档里要标一条待定：status 口径。我先按『跟 Excel 的值走』写…标成待确认」，用户点了一句（turn 23）它立刻撤：「是我绕了。导入接口自己一套逻辑，Excel 给什么写什么，status 也不例外」。**真值（亲自读 933 行文件确认）**：createDiscounts 151-166 按时间强制算 status；modDiscounts 211-253 是 **delete+insert**（215 删/252 setId(null)/253 插）且 226-231 也按时间强制算；**Excel 导入 509-535 只做字典校验，534 setStatus(tmp.get("优惠状态")) 直写 Excel 值** → 导入改更新这条线 status 听 Excel，页面那套是另一条链，不构成待定。**为什么三重机制都没拦**：①提问核对阈值「>2 条」，它这轮只 1 条 → 数量阈值对一条假问题天然失灵；②引用核对无效（该条无行号）；③提问纪律在场仍不够，且**我自己的措辞有问题**——〔需求解读〕的「问题预算：最多 2 个」像配额，诱导它凑一条待确认交差。**修法**：①去配额化（问题预算 →「提问的默认值是 0」；契约块 待确认 →「默认 0 条」；协议加「已核实的事实不要再挂待确认」）；②新增 core/signals.ts `auditOpenQuestions()`（抽「待用户拍板」条目：小节下的列表项 + 行内含 待定/待确认/待你定 的句子，**短标题行不算条目**（≤24 字且无句读）；逐条判有没有 `文件:行` 证据或「代码答不了」豁免（查不到/配置中心/登录不了/需要你提供）；一条没证据也顶，并引出原文；每会话 ≤2），buildQuestionAuditDirective 改收 {count, unsupported}。验证：405 用例（+5，turn22 真实文本为用例）· tsc 干净 · 门禁 27/27 · install-local 已覆盖逐字节相同。**同步记：本批次另一半生效了——06:06:39 首次调用 lume_contract（带 expectCount/criteria）、06:07:29 首次 lume_design，载具从 0 次变成用上了。**

# lume effect-verification carrier-usage doc-style user-preference
【重启后效果验证（turn 22–27，2026-09-23 14:0x–14:2x）】机械化那半确实起作用了：turn 23 **首次**调用 lume_contract（expectCount/criteria 都填了）与 lume_design；写文档后**回读核对**再 present；turn 24–27 模型自主做**残留检查**（grep 文档内「需求四|企微|WTPF_BS_ORDER」→ 零命中）并按契约出**对账表**，其中一条**诚实标注「偏离」**（「待补项单独列出」这条判据因需求四被剔除而不适用）。→ 结论：契约/载具类机制在真机上被用起来了；剩下的「判断力」类问题（status 那条）靠 auditOpenQuestions 继续顶。另记用户当场偏好：**文档里不要写「本文档不含 X」「不在本次范围」这类此地无银的声明，不相关的直接删**（已加入 buildDocumentMethodDirective，尾部注入零缓存成本）。

# lume coverage-check self-certification deliverable pending-restart
【自证不算验证 → 需求覆盖核对（2026-09-23 15:1x 已装本机待重启）】现场：交付文档自称「覆盖需求三全部条目，8 条全有落点，已验证」，但那对照表是模型自己切条目、自己填落点（无外部事实），等于自证；实际藏着三类硬伤（用户让 goose 复核才发现）：①与需求原文矛盾（需求第4条原文『历史数据的权限人和业务类型都需开发做批量数据导入---具体数据待运营梳理后提供』，文档写成『运营只出优惠编码+业务类型』『权限人不从 Excel 读，由后端写入』→ 等于把全部历史优惠权限人批量写成导入账号，且模板不改、无纠正入口）②论据错（文档称 resultMap 没映射 create_id，实际已映射，而这条恰是『必须另开一列』的决策论据）③落点错（让把 authUser 条件加在 WtpfGoodsPropertyDefMapper.xml 的 whereSql，而列表查询 qryDiscountsList 在 _exp.xml:51 用 whereGoodsA → 照做会静默失效）。**机制**：新增 src/core/coverage.ts（splitRequirementItems 按用户原文切条目并编号 / coverageRows 把交付物里提到该条的句子与原文并列 / hasFigureRefs / danglingSectionRefs 查悬空章号）+ buildRequirementCoverageDirective；产物正文取自 write/edit 的 tool args，只收 .md/.markdown/.txt；触发=文档产物+需求锚点≥2条，产物写出后的下一步即生效（同轮），每会话≤2次。**两个实测发现**：①需求锚点落账时**换行被压成空格**（真机 lume_project.json 就是『…字段 （1）列表页…』）→ 切条目必须按编号重建换行（对『见 2.6』不误切）；②强命中层最初永远为空——ngrams 只生成 3-gram 而强判据是 len≥4，测试当场抓到。**边界**：语义正确性机械判不了（下一步可做『否定性断言 vs 本次读到的代码』对齐）。验证 416 用例（+11）· tsc 干净 · 门禁 29/29 · install-local 已覆盖。

# lume coverage-corpus requirement-anchor-rotation ledger-truncation pending-restart effect-verified
【第八批生效验证 + 第九批修复（2026-09-23 16:1x-17:0x）】**第八批（真机 arguments 是 JSON 字符串）已生效**：16:17:56 重启、16:09 安装。真机证据：lume_project.json 该会话 ledger 从 undefined → **36 条**；注入侧〔改动台账〕×21、〔需求覆盖核对〕×6、〔交付对账〕×6、〔提问核对〕×4 全部出现——字符串入参修复救活了整批死代码（台账/引用核对/定位门槛/auto-verify/交付对账/覆盖核对）。**第九批两个问题（已修待重启）**：① 覆盖核对**切错语料**——08:27 那条写「需求原文共 3 条：1 原文：2.1.6 方案 A 漏了 consumer 侧的…」，它把用户粘贴的**评审意见当成需求**；根因两层：(a) 需求锚点是第 4 条，随后 8 段评审粘贴把表塞满，trimRequirements(cap=10) 只保「首条+最近」→ 需求原文被轮出表外；(b) 覆盖核对没有语料判别。修：core/coverage.ts 新增 looksLikeReview/isRequirementStatement/pickRequirementCorpus（挑不出需求原文就整段跳过，宁可不做也不做错）；core/ledger.ts trimRequirements 改分层保留（首条 + 有结构的需求原文优先，闲聊/评审先挤掉）；renderRequirements 标签说实话（需求原文 vs 用户原话）。② 台账路径**掐头**截断（10/36 条显示成 …\Wtpf、…GoodsManageControlle）→ 改保留尾部（文件名可见）。**行为亮点（turn 36–45）**：命名核对时承认「AUTH_USER/authUser 是我自己编的占位名、项目零先例」，你指出歧义后**自己撤回** CREATE_NAME 并给理由（同表已有 CREATE_ID/CREATE_DATE），定 PERMISSION_NAME；turn 44 开始写代码——lume_contract **首次在真实开发轮被调用**（带完成判据/非目标/待确认），29 edit + 13 read，git add 只加本次 8 个文件；turn 45 对评审反驳 1 条认同 3 条并用 grep 自证。验证 420 用例（+3）· tsc · 门禁 29/29 · install 已覆盖。

# lume architecture refactor host-events notice-slots prompt-blocks layering pending-restart
【架构重构 ①②③④（2026-09-23 17-18 时 · 已装本机待重启 · 零缓存成本）】用户看完架构评审后要求全做。**① 宿主事件适配层**：新增 src/host/host-events.ts，把所有宿主形状差异集中一处（toolNameOf/toolArgsOf/toolTargetOf/toolCommandOf/parseToolCall），真机 `{name, arguments:'<JSON 串>'}` 与假宿主 `{args:{…}}` 都支持；并从会话日志实录 test/fixtures/host-events/*.json（read/grep/glob/write/edit/pwsh + tool/result + user/message），test/host-events.test.ts 6 条断言——形状再变先红。**② 提示槽泛化**：新增 src/host/notices.ts（notices 表 + NOTICE_CAPS + setNotice/forceNotice/clearNotice/noticeText/noticeOpen），SessionRuntime 字段 57→43，每个机制只剩「何时生成」一处。**③ 拆 index.ts（部分）**：提示块装配抽出为 src/host/prompt-blocks.ts（carrierBlocks + volatileBlocks + 显式 BlockDeps），新增 test/prompt-blocks.test.ts（问答轮不得出现契约块、易变段顺序、覆盖核对上限）；index.ts 1723→1664 行；**未完成**：session-events（~300 行事件 switch）、tools（~130 行注册）、bootstrap（~200 行）三块纯搬迁，因有 10 批未提交改动、缺干净回滚点而暂缓，建议提交后继续。**④ 静态护栏**：修掉唯一分层破例 core/card.ts→host/identity.ts（容量/净化搬到 src/core/persona-limits.ts，identity 只再导出）；门禁加 layering 检查（core 不得依赖 host/client）→ 30/30；fire-and-forget 一律留痕：新增 projectTask(sid,label,run) 替换 void projectReady.then(...)（失败写 logger.warn）——这类写入原来失败不报错也不入账，正是「静默死」能活很久的原因。验证 431 用例（+5）· tsc 干净 · 门禁 30/30 · install 已覆盖。

# lume claim-gate assertion-vs-evidence sections negative-claims pending-restart
【第十二批：断言-证据对齐（claim gate）+ sections 搬出 · commit f2e52f2 · 未 push · 已装本机待重启】① **claim gate**：事故原型是 2026-09-23 文档里的「列表查询的 resultMap 里 create_id/modify_id 都没映射，查不出来」——事实是已映射（DO result 12-15 行），而这条错误论据正是「必须另开一列」这个核心决策的依据。引用核对接不了它（**它没给行号**），所以新增机械判据：否定断言（没/未/不存在/不支持…）指向代码符号时——(a) 符号本会话从未在任何工具结果里出现过 → unseen（必顶）；(b) 见过但断言没给行号、且句子带排除/决策措辞（例外·没映射·要你定）→ no-line（要求补行号）。实现：src/core/citations.ts 的 symbolsIn/recordSymbols/unsupportedClaims（限 2 次，走 notices 槽），session-events 在 assistant/message 生成、tool/result 累积「见过的符号」（st.seenSymbols，上限 4000）；门禁加 claim-gate 行（31 项）。**边界（诚实）**：机械判不了「看过但仍判断错」，只能挡「没看就断言」。(b) 分支就是为「看过却判断错」设计的兜底：迫使它给出行号，于是它必须真去读那段。② **host/sections.ts**：systemPrompt.section（会话恒定：人设段/思考协议）与 systemPrompt.context（每轮易变 + 工具失败提示）的注册全部搬出，注释写清三条通道不能混用（前缀缓存是核心成本约束）。index.ts 1118 → **1071 行**（原始 1723）。③ 测试：真实事故文本当验收用例（resultMap/create_id/modify_id 三符号、unseen/no-line/已给行号三条路径）。验证：437 用例（+6）· tsc 干净 · 门禁 31/31 · install 已覆盖。

